<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ISBN barcode scanning (S7) — mobile_scanner. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <!-- Local due-date reminders (flutter_local_notifications). POST_NOTIFICATIONS
         is required on Android 13+; RECEIVE_BOOT_COMPLETED reschedules pending
         reminders after a reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="kitabi"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- OAuth deep link back from the browser (Supabase Google sign-in) —
                 see core/auth/supabase_auth_service.dart. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="in.kitabi.kitabi" android:host="login-callback"/>
            </intent-filter>
            <!-- Shareable content links (kitabi.in/b|a|p/:id) → open in-app.
                 autoVerify uses the assetlinks.json at kitabi.in/.well-known;
                 the real signing SHA-256 must be filled in there first. Handled
                 by the content deep-link listener (core/deep_links.dart). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="kitabi.in" android:pathPrefix="/b/"/>
                <data android:scheme="https" android:host="kitabi.in" android:pathPrefix="/a/"/>
                <data android:scheme="https" android:host="kitabi.in" android:pathPrefix="/p/"/>
                <!-- www serves the same site; claim it so a shared www link
                     hands off too (deep_links.dart already accepts the host). -->
                <data android:scheme="https" android:host="www.kitabi.in" android:pathPrefix="/b/"/>
                <data android:scheme="https" android:host="www.kitabi.in" android:pathPrefix="/a/"/>
                <data android:scheme="https" android:host="www.kitabi.in" android:pathPrefix="/p/"/>
            </intent-filter>
        </activity>
        <!-- image_cropper (UCrop) — the crop screen shown after picking a cover /
             author photo / publisher logo. UCropTheme (styles.xml) opts this one
             screen out of Android 15's forced edge-to-edge — UCrop doesn't pad for
             insets, so its toolbar (incl. the confirm tick) drew under the status
             bar without it. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/UCropTheme"/>
        <!-- flutter_local_notifications: reschedule reminders after a reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open external https links (the [WIRED] buy-link
             redirect) in a browser on Android 11+. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
