# Python
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
api/.env
# Database dumps and the pre-launch wipe's undo snapshot — real reader data
# (emails, reviews, reading history). Never committed.
api/.backups/

# Flutter / Dart
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/.packages
app/pubspec.lock
app/ios/Pods/
app/ios/Podfile.lock
app/ios/**/xcshareddata/swiftpm/Package.resolved
app/android/.gradle/
app/android/local.properties
app/dart_defines.env
# Android release signing — secrets, never commit
app/android/key.properties
*.jks
*.keystore
# Riverpod/Drift codegen output — regenerated by build_runner, never committed
# (matches app/analysis_options.yaml's analyzer exclusion for the same files).
app/**/*.g.dart
app/**/*.drift.dart

# Claude Code
.claude/

# OS / editors
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
